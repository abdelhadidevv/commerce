import styled from "styled-components";

export const Price = styled.p`
  background: #fff;
  border-radius: 9px;
  padding: ${({ p }) => p && p};
  font-size: 18px;
  font-weight: 400;
  color: #0ea965;
`;
